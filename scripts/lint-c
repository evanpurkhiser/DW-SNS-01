#!/usr/bin/env bash
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
cd "$root"

resource_dir=$(clang -print-resource-dir)
clang-tidy \
	--extra-arg="-resource-dir=$resource_dir" \
	firmware/src/state_detector.c \
	-- \
	-std=c11 \
	-Ifirmware/src
